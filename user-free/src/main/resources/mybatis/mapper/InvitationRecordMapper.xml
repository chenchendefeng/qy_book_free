<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.only.tech.user.mapper.InvitationRecordMapper">

	<!--  查询用户账户信息 -->
	<select id="listInvitedCuid"  resultType="java.lang.Long" >
		select t.invitedCuid from invitation_record t where t.cuId = #{cuId}
	</select>

</mapper>