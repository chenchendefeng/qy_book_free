<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.only.tech.user.mapper.ReadFootprintMapper">

    <select id="getListByUid" resultType="com.only.tech.exposeinfz.user.dto.ReadFootprintDto">
        SELECT
            *
        FROM
            read_footprint
        WHERE cuId = #{cuId}
    </select>
</mapper>

